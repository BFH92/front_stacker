import { createTheme} from "@material-ui/core";

const LightTheme = createTheme({
  palette: {
    primary: {
      main: 'rgb(92, 30, 226)',
    },
    success: {
      main: 'rgb(76, 175, 80)',
    }
  },
});

export default LightTheme